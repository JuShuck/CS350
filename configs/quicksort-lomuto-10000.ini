[Sort]
; The name of the sort (Mergesort, Parallel Mergesort)
Name=Quicksort

; Any extra configuration options to pass, Quicksort needs to know it's
; partition type, for example.
Config=Lomuto

[Data]
Type=Sorted 
Size=10000
Iterations=1000